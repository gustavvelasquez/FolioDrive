# Nautilus fork (FolioDrive)

Build no **Ubuntu 24.04** (maintainer):

```bash
cd product
chmod +x scripts/build-foliodrive-files.sh forks/nautilus/apply-branding.sh
./scripts/build-foliodrive-files.sh
```

Produz `bundle/foliodrive-files-46.0-foliodrive.1.tar.gz` instalado em `/opt/foliodrive` com binário `foliodrive-files`.

Upstream pinado: **GNOME Nautilus 46.0** (Ubuntu 24.04). Promoção de versão nova só após teste no lab.

Licença upstream: **GPL-3.0-or-later**.
