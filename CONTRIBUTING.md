# Commits

Mensagens de commit neste repositório devem ser em **português do Brasil** (pt-BR).

Exemplos:

- `Documenta instalação via Release no README`
- `Corrige pré-check do instalador quando falta bundle`
- `Atualiza VERSIONS.json com checksums do SeaDrive 3.0.23`
