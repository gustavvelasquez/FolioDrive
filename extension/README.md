# Extensão SeaDrive (FolioDrive)

Fonte canônica no lab: [`forks/seadrive-ext`](../../forks/seadrive-ext/seadrive_extension.py).

O script `scripts/build-bundle.sh` copia `seadrive_extension.py` e `data/icons/` para `bundle/` no release.
